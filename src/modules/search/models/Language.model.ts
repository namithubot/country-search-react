/**
 * Denotes a language in the system.
 */
export interface Language {
    /**
     * Name of the language.
     */
    name: string;

    /**
     * Searchable code of the language.
     */
    code: string;
}
